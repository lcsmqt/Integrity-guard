# File Integrity Guard não usa APIs pagas.
# Este arquivo existe apenas para documentar variáveis locais opcionais.
# Copie para .env se quiser sobrescrever caminhos em scripts.

FIG_CONFIG=config/config.yaml
FIG_DB_PATH=data/integrity.db
FIG_REPORT_DIR=reports
FIG_LOG_LEVEL=INFO
